# PORT=3001

# Optional. If set, the server starts a local SOCKS5 listener that
# tunnels through this HTTP CONNECT proxy, so gramjs can reach Telegram
# servers via your existing HTTP proxy (e.g. Happ on 10809).
# TELEGRAM_HTTP_PROXY=http://127.0.0.1:10809
